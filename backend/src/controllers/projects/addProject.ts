import { PrismaClient } from "@prisma/client";
import { Request, Response } from "express";
const prisma = new PrismaClient();

const addProject = async (res: Response, req: Request) => {};

export default addProject;
